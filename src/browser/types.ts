import type { Si18nInitOptions } from "../core";

export interface Si18nBrowserInitOptions extends Si18nInitOptions {
  activeClass?: string;
  togglersSelector?: string;
  isTogglerSelect?: boolean;
}
